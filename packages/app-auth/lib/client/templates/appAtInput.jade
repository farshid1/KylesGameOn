template(name="appAtInput")
    .form-group.has-feedback
        input.form-control(type=options.type, placeholder=options.placeholder, id="at-field-{{_id}}" name="at-field-{{_id}}" autocapitalize="none" autocorrect="off")
        span.glyphicon.form-control-feedback(class=options.icon)